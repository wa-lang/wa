// 版权 @2022 凹语言™ 作者。保留所有权利。

import "examples/prime/benpkg"
import "joypkg"

func main {
	for n := 2; n <= 100; n = n + 1 {
		if isPrime(n) {
			benpkg.PrintInt(n)
		}
	}
	println("")

	for n := 2; n <= 100; n = n + 1 {
		if isPrime(n) {
			joypkg.PrintIntComma(n)
		}
	}
	println("")
}
