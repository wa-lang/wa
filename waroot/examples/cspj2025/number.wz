注: CSP 2025入门组第二轮 第1题

引入 "操作系统"

函数·主控:
	解题(操作系统.标准输入数据())
完毕

函数·解题(输入: []字节):
	数字表 := 构建([]整型, 10)

	注: 统计数字的个数
	循环 _, 某 := 迭代 输入:
		如果 某 >= '0' && 某 <= '9':
			数字表[某-'0']++
		完毕
	完毕

	注: 倒序输出
	循环 甲 := 9; 甲 >= 0; 甲--:
		循环 乙 := 0; 乙 < 数字表[甲]; 乙++:
			打印(字串('0' + 甲))
		完毕
	完毕
完毕
