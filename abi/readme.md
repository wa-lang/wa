# ABI 约定

- 面向用户定义了Target类型, 包含操作系统和CPU类型(隐含了ABI函数调用约定规范).
  - 包目录下以该Target默认名字后缀的文件将被包含(`*_${Target}.{wa|wz|wat|wa.s|wz.s|wa.c|wz.c|gcc.txt}`)
- 面向凹语言内部定义了ABI调用规范类型, 不对外部普通用户暴露.
  - 用于IR到后端汇编代码翻译时的参数
- 当前包不得依赖其他包
